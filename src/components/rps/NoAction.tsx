import { Heading, VStack, Text } from '@chakra-ui/react'

const NoAction = () => {
  return (
    <VStack m={'3rem'} justifyContent={'center'}>
      <Heading> No Action For Now</Heading>
      <Text>Request Opponent to Make the Move 🏃</Text>
    </VStack>
  )
}
export default NoAction
